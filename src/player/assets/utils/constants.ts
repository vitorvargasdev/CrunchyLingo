export enum States {
    PLAYING = 'playing',
    PAUSED = 'paused',
}

export enum Actions {
    PLAY = 'play',
    PAUSE = 'pause',
    READY = 'ready',
    ENDED = 'ended',
    TIME_UPDATE = 'timeupdate',
}

export enum LANGUAGE_TYPES {
    JAPANESE = 'japanese',
    NATIVE = 'native'
}

export enum LANGUAGES {
    ENGLISH = 'en-US',
    PORTUGUESE = 'pt-BR',
}

export enum SUBTITLE_ACTIONS {
    PREVIOUS = 'PREVIOUS',
    NEXT = 'NEXT',
    RELOAD = 'RELOAD'
}
